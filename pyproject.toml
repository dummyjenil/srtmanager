[project]
name = "srtmanager"
version = "0.2.0"
description = "A production-safe Python library for reading, editing, transforming, and saving .srt subtitle files."
readme = "README.md"
dependencies = ["srt"]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
